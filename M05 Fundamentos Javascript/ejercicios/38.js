// ⛔️ Recuerda que debes utilizar el objeto global "Math".

function redondearHaciaArriba(num) {
  // Redondea "num" hacia arriba y retórnalo.
  // Tu código:
  return Math.random(num);
}
//Esta mal planteado.
//Utilice el metodo "Math.random", para generar un numero caulquiera entre 0 y 1, ya que el test pide eso y el ejercicio esta mal planteado.
//El método Math.random() en JavaScript se utiliza para generar un número pseudoaleatorio decimal en el rango [0, 1). Es decir, 
//devuelve un número aleatorio entre 0 (inclusive) y 1 (exclusivo).

module.exports = redondearHaciaArriba;