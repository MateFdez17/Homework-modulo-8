function agregarItemAlFinalDelArray(array, elemento) {
  // Agrega el "elemento" al final del arreglo recibido.
  // Retorna el arreglo.
  // Tu código:
  array.push(elemento);
  return array;
  }

  //Agregue el elemento al final del array y retorne el array(con agregarlo con .push automaticamente va al final del arreglo)
module.exports = agregarItemAlFinalDelArray;
