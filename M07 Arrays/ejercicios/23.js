function devolverUltimoElemento(array) {
  // Retornar el último elemento del arreglo recibido.
  // Tu código:

  let ultimoElemento = array.length -1;

  return array[ultimoElemento];
}

//cree una variable para el ultimo elemento en el que lo almacene restandole 1 al largo del array,
// y luego lo retorne
module.exports = devolverUltimoElemento;
